# MovieBox API

Professional MovieBox API with session management and multi-endpoint failover.

## Endpoints
- GET /api/home - Homepage with all sections
- GET /api/sections - List all sections
- GET /api/search?q= - Search movies/TV
- GET /api/detail/:slug - Get movie details
- GET /api/stream/:id?detail_path= - Get stream URLs
